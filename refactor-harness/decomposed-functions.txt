# Functions the refactoring has deliberately restructured, by stage.
#
# Why this file exists
# --------------------
# The supplementary L0 check compares every function of the baseline SisProd.cpp
# against wherever it lives now, token for token. A stage that decomposes a
# function, adds a parameter or requalifies a call changes the token stream on
# purpose, and the check reports it. That is correct, and it is also noise that
# accumulates: by the tenth stage the report would carry a hundred permanent
# differences, and a function that vanished for a bad reason would be
# indistinguishable from one that moved as planned.
#
# Naming a function here says: this stage changed its token stream on purpose,
# and verified it some other way. Everything NOT named here must still be
# token-for-token identical. A name here that turns out to compare equal is
# reported as a stale declaration, so the list cannot rot.
#
# This is not a way to silence a failure. It is a way to keep the remaining
# failures worth reading.

# --- stage 1: drift-flux correlations -------------------------------------
# The five correlations were deduplicated (BhagwatGhajar and its Mod variant
# share a core) and the three aggregators take the resolved selector as a
# parameter instead of reading arq. Verified by the L1 sweep: 10125 correlation
# cases and 87480 aggregator cases, bit for bit.
BhagwatGhajar
BhagwatGhajarMod
Choi
HibikiIshii
FrancaLahey
C0UdDisperso
C0UdAnularChurn
C0UdEstratificado

# The five CalcC0Ud variants only requalify their calls into
# driftflux::correlations and read the cached selectors.
#
# --- stage 3 moved their bodies out entirely --------------------------------
# What is left in SisProd.cpp under these names is a delegation that builds a
# ClosureState and calls driftflux::coefficient. The bodies moved token for
# token, verified by c0ud-move.py, which inverts the substitution table and
# compares 13556 tokens against the commit the move started from -- the only
# check that reaches CalcC0UdBuf, CalcC0UdIni and CalcC0UdIniBuf, none of which
# the corpus executes. verify-c0ud.sh then covers behaviour across 7315 rows,
# calibrated against 14 injected defects.
CalcC0Ud
CalcC0UdBuf
CalcC0UdIni
CalcC0UdIniBuf
CalcC0UdPerm

# resolveDriftSelectors() is called from the three places arq is built.
SProd
operator=
copiaSemJson

# --- stage 4: trend output ------------------------------------------------
# The eight bodies moved to SisProdTrendOutput.cpp and were folded onto two
# skeletons; these are the forwarders that keep the public signatures (FR-032),
# since Num4Main.cpp calls four of them from 28 sites. Verified by
# verify-trend-writers.sh -- 864 files over 96 configurations, byte identical --
# which is the only check that reaches the four cross-section writers.
ImprimeTrendP
ImprimeTrendPCab
ImprimeTrendG
ImprimeTrendGCab
ImprimeTrendTransP
ImprimeTrendTransPCab
ImprimeTrendTransG
ImprimeTrendTransGCab

# --- stage 2: root-finding solvers ----------------------------------------
# The three solvers lost `prod` and `tipoCC` from every call: what was
# multMarcha(x, prod, tipoCC) is now objective(x), so the token stream differs
# from the baseline by construction at nineteen sites. zriddr also gained a
# monitor callable, and the two lines deriving minit from the input deck moved
# to the binding site.
#
# Verified two ways, because neither alone reaches everything. solver-move.py
# inverted the substitution table and compared 1615 tokens against the pristine
# commit exactly -- the only check that reaches zbrent and falsacorda, which
# have no call site in the project and never execute. verify-solvers.sh then
# swept all three over synthetic objectives, 912 configurations recording every
# evaluation in order, and that is what guards them after T037r renamed the
# locals and the token comparison stopped applying.
zbrent
# Renamed outright after the stage-2 gate cycle: falsacorda is Portuguese for
# "false chord", the regula falsi, and the body has never been that -- it takes
# the midpoint. Nothing calls it, so there is no forwarder under the old name
# and the check has to be told where the body went.
falsacorda -> bisect
zriddr

# SIGN and sign moved with their bodies untouched; only T037r's rename of their
# parameters changed the token stream. Declared because the supplementary check
# runs WITHOUT --allow-renames, so a rename reads there as a difference. With
# that flag they compare equal, and verify-solvers.sh exercises both directly --
# 25 SIGN cases and 5 sign cases, over the values where each one branches.
SIGN
sign

# multMarcha's four-deep conditional chain became a pure selector plus a flat
# switch. Verified by verify-dispatch.py: the original chain, carved out of the
# baseline commit, compiled beside the new selector and swept over 2240
# combinations of the five selectors, including the three rows no model reaches.
multMarcha

# --- stage 5: thermal calculation ----------------------------------------
# T061 moved the first three scalar thermal kernels to SisProdThermal.cpp.
# thermal-move.py inverts the signature and ThermalState substitutions and
# compares 3491 tokens exactly. verify-thermal.sh drives all three through the
# preserved public SProd methods; 43 rows remain byte-identical, and the
# calibration catches a corruption in each moved body.
interpolaHLatente
calcHmix
energmix

# T062 first moved calctemp whole and proved its 5937-token inverse against the
# pre-move commit. It was then split, one block at a time, into
# prepareTemperatureBalance, computeKineticTemperatureTerm and
# computeTemperatureSourceTerms. L2 passed after every split, the 43-row
# thermal sweep stayed byte-identical, and measure-functions.py reports zero
# functions above 200 lines.
calctemp

# T063 moved calcTempEntalp and calcTransMassTermo through the same invertible
# ThermalState substitution. The first compared equal over 465 tokens. The
# second compared equal over 3887 tokens before its mass-source block was
# extracted to satisfy SC-004; the decomposition checker verifies 2523 main
# tokens and 1986 helper tokens separately. calcTransMassTermo remains dead.
calcTempEntalp
calcTransMassTermo

# T064 first moved renovaTemp whole and proved its 7039-token inverse against
# the pre-move commit. It was then split into inlet initialization, property
# preparation, derivative updates, model selection and model application. The
# decomposition checker compares 8370 tokens across the main body and five
# helpers, and is calibrated against seven independent corruptions.
renovaTemp

# T065 first moved the renovaterm family whole and proved exact inverses of
# 11,783, 3,704 and 2,845 tokens. The main body was then split into interior
# processing, inlet/outlet boundary handling and finalization; the two buffered
# boundary functions each received a regime helper. The decomposition checker
# expands all seven helper calls and reconstitutes the three original bodies,
# comparing 18,332 tokens exactly. Its calibration catches nine independent
# corruptions across the coordinators and helpers.
renovaterm
renovatermAfluFim
renovatermColIni

# T066 moved both transient-energy functions whole. thermal-move.py reverses
# the ThermalState substitutions and direct coupling adapter, comparing 834 and
# 1,630 tokens exactly. Its calibration includes the qualified `dt` and
# `poissonSolver.renova()` cases so a member call cannot be mistaken for an
# unqualified SProd call again.
prepDifusCalorND
marchaEnergTrans

# T068 moved both steady temperature marches whole, proving inverses of 4362
# and 4097 tokens. T067 had shown they diverge in fifteen directional ways and
# thirteen non-unifiable logic ways, so they were NOT unified: the two legacy
# flows sit side by side with no reverseMode branch. An empirical search for
# the longest identical contiguous block between the two moved bodies found
# nine lines, all trivial zero-assignments, so nothing was shared.
#
# T071 then split each march into four helpers of its own -- sources, annulus
# coupling, kinetic term, latent term -- to satisfy SC-004. The helpers are per
# march and never shared, because L06, L09 and L11 make the reverse bodies
# genuinely different. check-steady-decomposition.py inlines all four calls in
# each march and compares 4924 and 4627 tokens against the pre-split commit;
# calibrate-steady-decomposition.sh confirms it catches nine independent
# corruptions.
#
# RenovaTempPermRev has ZERO coverage in the demo corpus, so L2 never enters
# its body: the token proof plus the dedicated harness are its authority.
RenovaTempPerm
RenovaTempPermRev

# T069 moved five thermal helpers. thermal-move.py inverts the ThermalState
# substitution and compares 1213, 649, 299, 69 and 269 tokens exactly, with the
# tokenizer calibrated against all seven forbidden transformations of the task
# table.
#
# tempDescarga is reached only through arq.descarga == 1 -> resolveDescarga(),
# a gas-lift unloading run no demo model enables, so neither L2 nor the harness
# reached it. The thermal sweep was extended for it before the move was
# committed: the reference table went from 73 to 80 rows, and the sweep built
# against the pre-move commit and against the moved code yields identical
# tables.
calctempGas
tempDescarga
TempDescGL
atualizaPeriTempProd
calcTempFim

# ---------------------------------------------------------------- estagio 6
# The gas-line and gas-lift domain. Each of these 22 kept its public signature
# -- FR-032 requires it and Num4Main.cpp calls them by name -- and its body was
# replaced by a single delegation to sisprod::gaslift. So L0 compares a body
# against a one-line call and reports DIFFERS for every one of them.
#
# Declaring them is not a way of silencing the check. Each body was proven
# token for token against the baseline by gaslift-move.py at the moment it was
# moved (T073-T079), which is a stronger statement than the L0 sweep makes: the
# sweep compares what is at the old address, and there is nothing there any
# more. L2 then confirms 14 of 14 models bit for bit.
#
# The suppression is narrower than a name on a list suggests, and this was
# checked in verify-structural.py rather than assumed:
#   - MISSING increments the failure count unconditionally, declared or not, so
#     a function that vanishes from every file still fails here;
#   - STALE fires when a declared function turns out to be identical again,
#     so an entry that outlives its reason reports itself instead of
#     accumulating silently;
#   - a body that changed during the move fails the token proof in its own
#     task, and any behavioural change fails L2 and gate 6.
# All this declaration suppresses is "the body at this address differs from the
# baseline body" -- which is the point of having moved it.
HidroDescargaG
renovaGas
renovaGasBuf
areaValvCali
resolveDescarga
avancInter
ValvGasTrans
prescordesc
CalcPresValvDesc
BuscaPresInjDesc
subtempoGas
subtempoGasBuf
conectaColuna
solveLinGas
delpGasPerm
delpInjPerm
RenovaPresGasPerm
calcVazGasPerm
IniciaVazValvGasPerm
RenovaTempGasPerm
conectaColunaPerm
IniciaconectaColunaPerm

# --- stage 7: steady march -------------------------------------------------
# Every name here is a body that moved to SisProdSteadyState.cpp and left a
# one-line delegation behind. L0 compares by name, finds the delegation, and
# reports a difference; that difference IS the move.
#
# What verified them instead, per body: steady-move.py applied the INVERSE
# substitution to the installed body and compared tokens with the pre-move
# commit -- 31 bodies, all OK -- and L2 was green 14/14 on the corpus after
# every step. Two of those token proofs caught real defects before the compiler
# did, so this is not a formality.
#
# The nine T090 and T092 helpers are NOT listed. They have no baseline
# counterpart, so L0 never compares them, and listing them would be a stale
# declaration the check rejects.
RenovaMassPerm
RenovaMassPermRev
RenovaMassPermComp
RenovaMassPermCompRev
RenovaPresPermMon
RenovaPresPermJus
RenovaPresPermNcel
calcDpArea
corrDeng
RenovaTransMassPerm
RenovaTransMassPermGas
marchaProdPerm1
marchaProdPerm1Rev
marchaProdPerm2
marchaProdPresPres1
marchaProdPresPres1Rev
marchaProdPresPres2
marchaProdPresPres3
marchaGasPerm1
marchaGasPerm2
marchaGasPerm3
marchaInjPerm1
hidroreverso
hidroreversoInj
hidroTramoSecundario
hidroLinServ
atualizaProp
atualizaVelTermPerm
calcDTPseudoTrans
atualizaPeriPmonProd
atualizaPeriPjusProd
